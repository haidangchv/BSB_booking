# Verification & Browser Policy

- Do NOT use `browser_subagent` or take browser screenshots unless explicitly requested by the user.
- Verify code changes strictly using terminal commands (`npm run lint` or `tsc --noEmit`).
- Keep responses concise, direct, and focused on code implementation.
